<div class="Interrupt Interrupt-jobs">
  <div class="l-new-wrap l-event-groups">
    <div class="l-event-group"></div>
    <div class="l-new-wrap l-events-container">
      <h2 class="th1 Interrupt__title">{{date.title}}</h2>
      <div class="Interrupt__jobs jsJobsPlacement l-events"> </div>
      <p class="h6 Interrupt__cta">
        <a href="{{date.link}}" class="btn">{{date.button}}</a>
      </p>
    </div>
  </div>
</div>